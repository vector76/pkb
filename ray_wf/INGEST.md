---
allowed_transitions:
  - { tag: reset, target: WAIT }
---

Create a new markdown file in issues/ and within that document, state that 
ingest is not yet implemented.  Be sure not to overwrite an existing file.  
Write one new file in that folder and do not touch any existing files.

Do nothing else.
